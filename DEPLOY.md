# أين تُستضيف النظام؟ (دليل النشر)

النظام حالياً **غير مستضاف على أي خادم** — الشيفرة فقط على GitHub، و`./run.sh` يشغّله على جهازك
للتجربة. لتشغيله فعلياً للموظفين، اختر أحد الخيارات الثلاثة أدناه. الاختيار يعتمد على سؤال واحد:

> **هل أجهزة البصمة تدعم وضع الدفع (Cloud Server / ADMS)؟**
> - نعم → استضافة سحابية (الخيار الأول) — الأسهل والأفضل.
> - لا، تدعم السحب فقط (المنفذ 4370) → خادم داخل الشركة (الخيار الثاني).

| | أين يعمل | وضع الجهاز | يصلح للعمل عن بُعد | التكلفة الشهرية |
|---|---|---|---|---|
| **1. خادم سحابي (VPS)** | مزود سحابي | دفع ADMS | ✅ نعم | ٥–٢٠ $ |
| **2. جهاز داخل الشركة** | Mini PC / سيرفر صغير | سحب 4370 + دفع | ✅ عبر Cloudflare Tunnel | شراء الجهاز فقط |
| **3. هجين** | VPS + وكيل داخلي | سحب داخلي | ✅ نعم | ٥ $ + جهاز صغير |

---

## الخيار الأول: خادم سحابي (VPS) — الموصى به

**المواصفات الكافية:** 1 vCPU / 1 GB RAM / 20 GB SSD (يكفي لمئات الموظفين؛ النظام عملية بايثون
واحدة + SQLite). مزودون مناسبون: Hetzner، DigitalOcean، Contabo، AWS Lightsail، Oracle Cloud
(طبقة مجانية)، أو مزود محلي داخل السعودية (STC Cloud / Alibaba Cloud KSA) إن كانت هناك متطلبات
لبقاء البيانات داخل المملكة.

**الخطوات:**

```bash
# على الخادم بعد ربط النطاق (A record) بعنوان IP الخادم
git clone https://github.com/moom11/123.git && cd 123
sudo bash deploy/install-ubuntu.sh hr.example.com
```

السكربت ينفّذ كل شيء: تثبيت المتطلبات، إنشاء مستخدم خدمة، بيئة بايثون، تهيئة قاعدة البيانات،
توليد مفتاح وكلمة مرور مدير عشوائية في `/etc/hr.env`، تسجيل خدمة systemd، إعداد Nginx،
وإصدار شهادة SSL عبر Let's Encrypt.

**ثم اضبط أجهزة البصمة على وضع الدفع:** من الجهاز `Comm ← Cloud Server / ADMS`:
- `Server Address` = نطاقك أو IP الخادم
- `Server Port` = `80`  (أغلب أجهزة ZKTeco لا تدعم HTTPS، ولهذا يُبقي إعداد Nginx المرفق مسار
  `/iclock/` على HTTP بينما يوجّه بقية الموقع إلى HTTPS)
- قيّد الوصول لعناوين أجهزتك بأسطر `allow` في `deploy/nginx.conf`.

**نُسخ Docker (بديل):**

```bash
cp .env.example .env && nano .env      # اضبط HR_SECRET_KEY و HR_ADMIN_PASSWORD
docker compose up -d --build
```

---

## الخيار الثاني: خادم داخل الشركة (On-Premise)

مناسب إذا كانت الأجهزة تدعم **السحب فقط**، لأن الخادم يجب أن يكون في نفس شبكة الأجهزة ليصل
إلى المنفذ `4370`. يكفي جهاز صغير دائم التشغيل (Mini PC، أو Raspberry Pi 4/5، أو خادم موجود).

```bash
sudo bash deploy/install-ubuntu.sh          # بدون نطاق
# فعّل المزامنة التلقائية:
sudo sed -i 's/HR_AUTO_SYNC_MINUTES=0/HR_AUTO_SYNC_MINUTES=10/' /etc/hr.env
sudo systemctl restart hr
```

**للوصول من خارج الشركة (وللحصول على HTTPS المطلوب لتحديد الموقع)** استخدم Cloudflare Tunnel —
لا يحتاج IP ثابتاً ولا فتح منافذ في جدار الشركة:

```bash
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
  -o /usr/local/bin/cloudflared && chmod +x /usr/local/bin/cloudflared
cloudflared tunnel login
cloudflared tunnel create hr
cloudflared tunnel route dns hr hr.example.com
cloudflared tunnel run --url http://127.0.0.1:8000 hr
```

النتيجة: `https://hr.example.com` يعمل للموظفين من أي مكان، والاتصال بأجهزة البصمة يبقى داخلياً.

---

## الخيار الثالث: هجين (VPS + وكيل داخلي)

إذا أردت الاستضافة السحابية مع أجهزة لا تدعم الدفع: يبقى التطبيق على VPS، ويُشغَّل داخل الشركة
جهاز صغير يسحب البصمات من الأجهزة ويرسلها إلى الخادم. **هذا الوكيل غير مبني حالياً** — أخبرني
إن أردته وسأضيفه (سكربت بايثون صغير يعمل بجدولة، يستخدم `pyzk` ثم يرسل السجلات إلى
`POST /iclock/cdata` بنفس صيغة الأجهزة).

---

---

## الاستضافة المجانية: ما يصلح وما لا يصلح

| الخيار | يصلح؟ | الملاحظات |
|---|---|---|
| **Oracle Cloud Always Free** | ✅ الأفضل | حتى 4 أنوية ARM و24 GB ذاكرة **مجاناً دائماً**، وفيه **منطقة جدة** (me-jeddah-1) بزمن استجابة ~20 مللي ثانية |
| **جهاز داخل الشركة + Cloudflare Tunnel** | ✅ | مجاني تماماً عدا الكهرباء، وهو **الخيار الوحيد الذي يدعم وضع السحب** (المنفذ 4370) |
| **Google Cloud e2-micro** | ⚠️ مقبول | نواة مشتركة + 1 GB ذاكرة، ومناطقه المجانية أمريكية فقط (~200 مللي ثانية)، وحد صادر 1 GB شهرياً. أضف swap بحجم 2 GB |
| **Fly.io** | ⚠️ مقبول | حصة صغيرة مع قرص دائم 3 GB يكفي قاعدة SQLite |
| **AWS Free Tier** | ⚠️ مؤقت | سنة واحدة فقط ثم يصبح مدفوعاً |
| **Render / Railway (المجاني)** | ❌ | الخدمة **تنام عند الخمول** فتضيع بصمات الأجهزة، ونظام الملفات مؤقت فتُمحى قاعدة SQLite |
| **PythonAnywhere (المجاني)** | ❌ | يمنع الاتصالات الشبكية الحرة، فلا يعمل السحب من أجهزة البصمة |
| **ProFreeHost وأمثاله** | ❌ | استضافة PHP/MySQL مشتركة: لا تشغّل بايثون ولا عملية دائمة ولا SSH. وبيانات الرواتب والهويات لا توضع على استضافة مجانية مشتركة |

> قاعدة عامة: أي استضافة **تنام عند الخمول** أو **نظام ملفاتها مؤقت** غير صالحة، لأن أجهزة البصمة
> ترسل طوال اليوم والقاعدة يجب أن تبقى. إن اضطررت لواحدة منها، اربط قاعدة PostgreSQL مجانية
> (Neon أو Supabase) عبر `HR_DATABASE_URL`.

### خطوات Oracle Always Free بالتفصيل (الموصى به)

1. **المنطقة:** اختر `me-jeddah-1` عند إنشاء الحساب — لا يمكن تغيير المنطقة الرئيسية لاحقاً.
2. **الجهاز:** `VM.Standard.A1.Flex` بنواتين و12 GB (نصف الحصة المجانية)، صورة Ubuntu 24.04.
   إن ظهر **"Out of host capacity"** — وهو شائع مع ARM — أعد المحاولة لاحقاً أو استخدم
   `VM.Standard.E2.1.Micro` مؤقتاً.
3. **افتح المنافذ في مكانين** (أشهر خطأ عند مستخدمي Oracle):
   - الشبكة: VCN ← Security List ← Ingress للمنفذين 80 و443 من `0.0.0.0/0`.
   - داخل الخادم، لأن صور Ubuntu على Oracle تأتي بقواعد iptables تحجب كل شيء عدا SSH:
     ```bash
     sudo iptables -I INPUT 6 -p tcp --dport 80 -j ACCEPT
     sudo iptables -I INPUT 6 -p tcp --dport 443 -j ACCEPT
     sudo netfilter-persistent save
     ```
4. **احجز IP ثابتاً** (Reserved Public IP) حتى لا يتغير بعد إعادة التشغيل.
5. **ثبّت النظام:** `sudo bash deploy/install-ubuntu.sh hr.example.com` — معمارية ARM لا تُشكّل
   مشكلة لأن كل الحزم بايثون خالص ولها حزم جاهزة لـ aarch64.
6. أبقِ الحساب غير خامل، أو رقّه إلى Pay As You Go مع البقاء داخل الحد المجاني، لتجنّب استرداد
   Oracle للموارد الخاملة.

### أين يقع Cloudflare من هذا؟

Cloudflare **طبقة أمام النظام وليس مكاناً لتشغيله**:

- **Workers / Pages:** بيئة JavaScript بلا نظام ملفات ولا اتصالات TCP/UDP خام → لا تعمل FastAPI
  ولا SQLite ولا بروتوكول ZKTeco. تشغيله عليها يعني إعادة كتابة النظام بالكامل وخسارة وضع السحب.
- **Cloudflare Tunnel:** ✅ الاستخدام الصحيح — نطاق وHTTPS مجاناً لخادم داخل الشركة بلا IP ثابت
  ولا فتح منافذ في جدار الشركة.
- **DNS + CDN + WAF أمام VPS:** ✅ مفيد ومجاني.
- **تنبيه:** لا تمرّر مسار `/iclock/` عبر بروكسي Cloudflare (السحابة البرتقالية) — أجهزة ZKTeco
  ترسل HTTP بسيطاً وقد تفشل. اجعل نطاق الأجهزة `DNS only` أو أبقِ اتصالها داخلياً.

### النطاق المجاني

إن لم ترد شراء دومين (~10 $ سنوياً): استخدم **DuckDNS** مع شهادة Let's Encrypt، أو نطاقاً فرعياً
عبر Cloudflare Tunnel. النطاق مطلوب لأن **HTTPS شرط لعمل تحديد الموقع** في متصفحات الجوال.

## بعد النشر مباشرة

1. ادخل بحساب `admin` وكلمة المرور المطبوعة من السكربت، وغيّرها من «حسابي».
2. الإعدادات ← الإدارات والورديات وأنواع الإجازات والعطل الرسمية.
3. الإعدادات ← مواقع العمل: أضف موقع المقر (زر «التقاط موقعي الحالي» من داخل المقر).
4. الموظفون: أضفهم برقم يطابق رقم المستخدم (PIN) في جهاز البصمة، أو استوردهم من الجهاز عبر
   «أجهزة البصمة ← المستخدمون ← استيراد».
5. أنشئ حساب دخول لكل موظف من زر «حساب دخول» في صفحة الموظفين.

## النسخ الاحتياطي والتحديث

```bash
# نسخة احتياطية يومية (القاعدة + المرفقات)
sudo tar czf /var/backups/hr-$(date +%F).tar.gz -C /opt/hr data
# تحديث النظام من GitHub
cd /path/to/repo && git pull && sudo bash deploy/install-ubuntu.sh hr.example.com
```

> قاعدة SQLite تكفي لأغلب الشركات. للأحجام الكبيرة أو الخوادم المتعددة، انقل إلى PostgreSQL
> بضبط `HR_DATABASE_URL=postgresql+psycopg://user:pass@host/hr` في `/etc/hr.env`.

## قائمة تحقق أمنية قبل التشغيل الفعلي

- [ ] تغيير كلمة مرور `admin` وضبط `HR_SECRET_KEY` (يفعله السكربت تلقائياً).
- [ ] HTTPS مفعّل — بدونه لن يعمل تحديد الموقع على جوالات الموظفين.
- [ ] تقييد مسار `/iclock/` بعناوين أجهزة البصمة فقط.
- [ ] جدار ناري: افتح 80/443 فقط، وأغلق 8000 من الخارج.
- [ ] نسخ احتياطي مجدول لمجلد `data/`.
